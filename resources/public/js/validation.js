
 function validateInsertForm() {

/*     var price = document.forms["frm"].elements["price"].value;
  alert(document.forms["frm"]["price"]);
    if (price == null || price == "" || (isNaN(price))) {
        alert("Please enter valid price.");
        return false;
    }
    var description = document.forms["frm"]["des].value;
    if (description == null || description == "") {
        alert("Please enter description.");
        return false;
    } */
  return true;
  }
